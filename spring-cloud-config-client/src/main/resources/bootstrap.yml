spring:
  application:
    name: cloud-client

  cloud:
    config:
      label: master
      uri: http://localhost:8768
      username: m15291092760@163.com
      password: Li691212
      discovery:
        enabled: true
        service-id: config-server
    bus:
      refresh:
        enabled: true
    rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest

server:
  port: 8769

#health:
#  config:
#    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: '*'
